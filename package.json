{
  "name": "may-coffee",
  "version": "1.0.0",
  "private": true,
  "description": "May Coffee monorepo (frontend + backend)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:network": "concurrently \"npm run dev:network --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
